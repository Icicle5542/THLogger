# Disable the deprecated NCS Partition Manager.
# NVS partition layout is defined entirely in the DTS overlay
# (boards/xiao_nrf54l15_nrf54l15_cpuapp.overlay, &storage_partition).
SB_CONFIG_PARTITION_MANAGER=n
